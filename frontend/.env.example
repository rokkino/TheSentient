# API base URL (usato a build time).
# In dev: .env.development imposta VITE_API_URL=/api (proxy Vite).
# In prod: .env.production imposta VITE_API_URL=https://thesentient.duckdns.org/api.
# Non serve impostare VITE_WS_URL: ws/wss viene derivato dal protocollo (vedi src/utils/env.js).
