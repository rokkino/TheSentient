# Backend API base URL (including /api). Used at build time for production.
# Leave unset for local dev (Vite proxy) or use relative /api.
# Production (HTTPS, avoids Mixed Content):
# VITE_API_URL=https://thesentient.duckdns.org/api
